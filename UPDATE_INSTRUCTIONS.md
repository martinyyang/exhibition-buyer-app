# 应用更新说明

## 已完成的修改

### 1. 密码输入框添加眼睛图标 👁️
- 点击可显示/隐藏密码
- 位置：密码输入框右侧

### 2. 放宽登录密码验证规则
- 旧规则：至少8位，必须包含大小写字母+数字
- 新规则：只需至少6位
- 原因：`remote123456` 没有大写字母，无法通过旧规则

## 如何看到更新

### 方法1：热重启（推荐）
在运行Flutter的终端中按：
- `r` = 热重载 (Hot Reload)
- `R` = 热重启 (Hot Restart) ⭐ 推荐

### 方法2：完全重启
```bash
# 停止当前运行
按 q 或 Ctrl+C

# 重新运行
flutter run -d chrome
```

### 方法3：强制刷新浏览器
- Windows: Ctrl + Shift + R
- Mac: Cmd + Shift + R

## 登录信息

成功更新后，使用以下信息登录：

- 邮箱：`remote@123.com`
- 密码：`remote123456` （现在可以通过验证了）

## 验证点

更新成功后，你应该看到：
1. ✅ 密码输入框右侧有眼睛图标
2. ✅ 点击眼睛可以显示/隐藏密码
3. ✅ 输入 `remote123456` 不会提示密码格式错误
4. ✅ 可以成功登录

## 代码修改位置

文件：`lib/features/auth/screens/login_screen.dart`
- 第20行：添加 `_obscurePassword` 状态变量
- 第41-50行：简化密码验证规则
- 第129-147行：添加眼睛图标和切换逻辑
